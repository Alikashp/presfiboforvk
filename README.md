# deckwright

Читает произвольный `.pptx` как набор правил дизайн-системы и собирает по нему
новые слайды: три варианта вёрстки на одном контенте, нативные объекты в
`.pptx`, встроенный аудит качества, экспорт в `.pdf` и `.html`.

Кейс VK Tech «Цифровой дизайнер презентаций», ЛЦТ 2026.

## Статус

В разработке. Пройдены фазы 0–11: пайплайн, аудит с циклом исправления и
веб-интерфейс. См. `progress/handoff.md`.

## Запуск

```bash
deckwright doctor                       # проверить системные зависимости
deckwright run \
  --config configs/config.yaml \
  --template путь/к/шаблону.pptx \
  --content контент-пакет.json \
  --variant balanced                    # без флага — все три варианта
```

Без ключа модели прогон идёт по записанным ответам: добавьте
`--recorded tests/fixtures/recorded`.

Бюджет ТЗ — **пять минут на генерацию трёх вариантов вместе**; разбор
входа (шаблон и контент-пакет) в него не входит. Последняя строка вывода и
`outputs/<прогон>/run-summary.json` показывают обе цифры раздельно:

```
[прогон] разбор входа 1.9с (вне бюджета); генерация вариантов (3) 34.3с из 300с (в бюджете)
```

**Что делать с находками аудита** — флаг `--fix` (по умолчанию режим из
`config.yaml`, там `review`):

| режим | что делает |
|---|---|
| `review` | показывает отчёт с ключами находок и **не трогает колоду**: выбирает человек |
| `auto` | применяет находки с исправлением `automatic` и пересобирает колоду |
| `off` | не запускает цикл исправления |

Находки, требующие решения человека (`assisted`) и ответы модели (контекстные),
не применяются ни в одном режиме — только явным выбором в интерфейсе. Подробнее
в [`docs/AUDIT.md`](docs/AUDIT.md), раздел «Цикл исправления».

## Готовые примеры

Девять презентаций — три шаблона датасета (`data/templates/`) × три варианта
вёрстки. В репозитории лежат их `.pdf`, отчёты аудита и манифесты:
[`outputs/demo/`](outputs/demo/). Сами `.pptx` весят 132 МБ (композиции
донора клонируются вместе с картинками) и выложены отдельно:

**Скачать `.pptx`:** _ссылка будет добавлена_

## Сетап

### Через Docker (рекомендуется)

Образ несёт LibreOffice Impress, poppler и шрифты — без них конвертация и
измерение текста не работают.

```bash
cp .env.example .env      # заполнить ключи
docker compose up --build
```

Интерфейс — http://localhost:8501

### Локально

```bash
cp .env.example .env
sh scripts/install-system-deps.sh   # системные пакеты: один список на все окружения
pip install -e ".[dev]"
streamlit run app/ui.py
```

Список системных пакетов лежит в одном месте — `scripts/install-system-deps.sh`,
— и его же зовут образ и CI. Держать его в трёх файлах значит однажды их
разъехать: без `libreoffice-impress` один `libreoffice-core` отвечает на `.pptx`
«source file could not be loaded», и это выглядит как поломка кода.

Проверить окружение: `deckwright doctor`.

Сессии Claude Code на вебе готовят окружение сами — `.claude/hooks/session-start.sh`
ставит тот же список, зависимости проекта и собирает синтетические шаблоны для
тестов. Если хук не подключён, то же самое делается руками командами выше.

## Переменные окружения

Все секреты — только в `.env`, шаблон в `.env.example`. В `configs/config.yaml`
лежат ссылки `${VAR}`, значений там нет.

| переменная | назначение |
|---|---|
| `LLM_BASE_URL`, `LLM_API_KEY`, `LLM_MODEL` | OpenAI-совместимый endpoint модели с открытыми весами |
| `VLM_BASE_URL`, `VLM_API_KEY`, `VLM_MODEL` | модель со зрением для контекстного аудита; может совпадать с LLM |
| `T2I_BASE_URL`, `T2I_API_KEY`, `T2I_MODEL` | text-to-image; по умолчанию выключено |

## Веб-интерфейс

```bash
streamlit run app/ui.py        # или docker compose up --build
```

Сценарий: загрузить шаблон, при желании свой контент-пакет, выбрать варианты и
нажать «Собрать». Прогон идёт в фоне, его номер остаётся в адресе — страницу
можно обновить, не потеряв результат. Находки аудита показаны рамками поверх
слайда; номер рамки совпадает с номером в списке. Отмеченное применяется
кнопкой, после чего колода пересобирается и проверяется заново, а что
применить не удалось — перечислено с причиной.

## Ограничения

- Вход — только `.pptx` и `.potx`. PDF, Keynote и Google Slides напрямую не
  принимаются.
- Только десктопные браузеры. Сценарий проверен живым прогоном в **Chromium**;
  Firefox, Safari и Яндекс Браузер в среде разработки отсутствуют и не
  проверены.
- Сервис не является редактором слайдов: он показывает найденные проблемы и
  чинит выбранные, но не даёт двигать блоки руками.
- Если встроенный в шаблон шрифт распаковать не удалось, подставляется
  метрически близкий; факт подстановки пишется в манифест прогона и в аудит.
- Если в шаблоне нет ни одного примера графика, форма графика будет наша, а
  цвета и типографика — шаблона. Это отражается в отчёте аудита.
- Генерация требует внешнего endpoint'а модели: GPU локально не нужен, но без
  доступа к модели работает только разбор шаблона.

## Документация

| | |
|---|---|
| пайплайн и границы слоёв | [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) |
| модели, лицензии, требования | [`docs/MODELS.md`](docs/MODELS.md) |
| проверки аудита и покрытие | [`docs/AUDIT.md`](docs/AUDIT.md) |
| контракт и критерии приёмки | [`specs/spec.md`](specs/spec.md) |
| исследование и обоснование архитектуры | [`research/research.md`](research/research.md) |
