services:
  deckwright:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      # Артефакты переживают пересборку образа.
      - ./outputs:/app/outputs
      - deckwright-cache:/app/.cache

volumes:
  deckwright-cache:
